{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Run Processing Sketch",
      "request": "launch",
      "mainClass": "gamecode.MainSketch",
      "cwd": "${workspaceFolder}",
      "classPaths": [
        "${workspaceFolder}/bin",
        "${workspaceFolder}/lib/core.jar"
      ],
      "preLaunchTask": "Compile Sketch"
    }
  ]
}
